{% extends 'base.html.twig' %}

{% block body %}
    <h2 class="page-header">{{ "todo.edit.title" | trans }}</h2>
    {{form_start(form)}}
    {{form_widget(form)}}
    {{form_end(form)}}
{% endblock %}
